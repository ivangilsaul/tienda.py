from setuptools import setup, find_packages

setup(
    name="tienda_virtual",
    version="0.1",
    description="Paquete para modelar clientes en una tienda virtual",
    author="Tu Nombre",
    packages=find_packages(),
    install_requires=[],
    classifiers=[
        "Programming Language :: Python :: 3"
    ]
)